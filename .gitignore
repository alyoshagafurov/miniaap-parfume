# dependencies
node_modules/
.pnpm-store/

# next
.next/
out/
next-env.d.ts

# build / runtime
dist/
build/
*.tsbuildinfo

# env — never commit secrets
.env
.env.*
!.env.example

# test / qa artefacts
coverage/
playwright-report/
test-results/
/qa/**/*.png
# Кроме иллюстраций к docs/HANDOFF.md: это часть документа для клиента, а не
# улика прогона. Девять кадров на 660 КБ, снимаются одним спеком и меняются
# только вместе с самой админкой.
!/qa/final/handoff/*.png
# Машинные артефакты прогона: отчёт, трассы, сводка. Пересоздаются pnpm test:e2e.
/qa/stage-1/playwright-results.json
/qa/final/playwright-results.json
/qa/**/*.webm
/qa/**/*.zip

# local data
/docker/data/
*.log

# os / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# impeccable local state
.impeccable/config.local.json
.impeccable/live/

# transient probe files written by research subagents into the repo root
__tx_probe*
tsconfig.tsbuildinfo
relay-stub.log

# Ручные дампы базы (scripts/dump.sh)
/backups/
